import { defineConfig } from 'astro/config';

// https://astro.build/config
import { defineConfig } from 'astro/config'

export default defineConfig({
  site: 'https://slateblua.github.io/vanakatu',
  base: 'vanakatu',
});